# from functools import reduce

# lista = [1, 2, 3]

# resultado= reduce(lambda x, y: x + y, lista)
# print(resultado)

# listaMap = [1, 2, 3]
# resultado = list(map(lambda x: x*2, lista))

# print(resultado)


# array = [1, 2, 3, 4]
# resultado = list(filter(lambda x: x != 4, array))
# print(resultado)

# for i in array:
#     print("hola")